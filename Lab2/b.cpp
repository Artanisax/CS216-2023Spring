#include <iostream>
#include <cmath>

using namespace std;

void IO_accel()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
}

const int N = 3.2e4;

bool ans[N] = {0, 1};

int main()
{
    IO_accel();
    
    return 0;
}